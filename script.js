const header=document.querySelector('.header');const menu=document.querySelector('.menu');menu.addEventListener('click',()=>{const open=header.classList.toggle('open');menu.setAttribute('aria-expanded',open)});document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>{header.classList.remove('open');menu.setAttribute('aria-expanded','false')}));document.querySelector('form').addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.currentTarget);const subject=encodeURIComponent(`Solicitud web · ${d.get('tipo')} · ${d.get('localidad')}`);const body=encodeURIComponent(`Nombre: ${d.get('nombre')}\nTeléfono: ${d.get('telefono')}\nEmail: ${d.get('email')}\nLocalidad: ${d.get('localidad')}\nTipo: ${d.get('tipo')}\n\n${d.get('mensaje')}`);location.href=`mailto:hola@onicpavimentos.es?subject=${subject}&body=${body}`});
